# this is ansible-galaxy dependencies files
# use it as:
# ansible-galaxy install -r requirements.yml

---
- src: geerlingguy.docker
  version: 2.5.2
- src: naftulikay.google-cloud-sdk
  version: v1.0.0
